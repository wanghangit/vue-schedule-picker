{
  "name": "vue-schedule-picker-monorepo",
  "version": "2.0.4",
  "scripts": {
    "dev": "pnpm run --filter=demo dev",
    "build": "pnpm run --filter=vue-schedule-picker build",
    "build:demo": "pnpm run --filter=demo build"
  }
}
